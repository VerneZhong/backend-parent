eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8080

zuul:
  routes: # 路由规则
    film: # application.name -> 服务名称 service-id
      path: /film-api/**  # 匹配规则
#    service-id: film
